@layout('layouts/main')
@set('title', 'Expenses Type')
@set('breadcrumbMainTitle', 'Expenses-Type')
@set('breadcrumbSubTitle', 'New Expense-Type')

@section('body')
  <div class="row">
    <div class="offset-md-3 col-md-6">
      <div class="card card-primary">
        <div class="card-header">
          <h3 class="card-title">New Expense-Type</h3>
        </div>
        <form action="{{ route('expensetypes.store') }}" method="POST">
          @include('expensetypes/form')
        </form>
      </div>
    </div>
  </div>
 @end
